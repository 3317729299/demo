<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mybatisPlus.mapper.WorkTimeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.demo.mybatisPlus.model.WorkTime">
        <id column="id" property="id"/>
        <result column="work_shop" property="workShop"/>
        <result column="production_line" property="productionLine"/>
        <result column="shift" property="shift"/>
        <result column="serial_number" property="serialNumber"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, work_shop, production_line, shift, serial_number, start_date, end_date
    </sql>

</mapper>
